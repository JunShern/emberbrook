// route_overlay.js — legibility dev overlay (STUB; owned by the legibility auditor).
// Contract: toggle with 'R'. Draws, per shot/scene: entry points (green), exit
// points (orange), and the authored route polyline, sourced from
// public/townmap/dellhollow.routes.json. Must no-op silently when the data file
// is absent or the scene has no routes. Never touches game state.
(function(){ /* stub — auditor fills this in */ })();
